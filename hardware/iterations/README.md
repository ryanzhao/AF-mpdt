# Hardware Iterations

This directory stores historical, draft, and legacy MPDT hardware design files.

- `mpd-v1/` is reserved for curated MPD v1 design iteration files.
- `early-drafts/` is reserved for early concept and draft exports.
- `legacy-uploaded-files/` contains previously uploaded files whose exact version status is uncertain.
